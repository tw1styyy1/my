import * as React from 'react'
import './header.scss';

const Header: React.FC = () => {
    return (
        <div className="App">
            <p>привет</p>
        </div>
    );
}

export {Header};
